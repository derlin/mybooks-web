- never commit anything, I am handling this
- never start the dev server unless explicitly asked, and do not stop it unless you started it yourself
- if needed, use `npm run build`, `npm run test`, `npm run typecheck` for checks
- reusable styles are in `style.css`, reusable functions in `utils/*.ts`
- always use Lucide icons, no unicode symbols
